version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 AMPLIFY DEBUG SESSION STARTED 🔧"
        - echo "========================================="
        - echo "NODE_ENV=$NODE_ENV"
        - echo "PWD=$(pwd)"
        - echo "USER=$(whoami)"
        - echo "========================================="
        - echo "DATABASE_URL check:" && test -n "$DATABASE_URL" && echo "✅ DATABASE_URL EXISTS" || echo "❌ DATABASE_URL MISSING"
        - echo "NEXTAUTH_SECRET check:" && test -n "$NEXTAUTH_SECRET" && echo "✅ NEXTAUTH_SECRET EXISTS" || echo "❌ NEXTAUTH_SECRET MISSING"
        - echo "S3_ACCESS_KEY_ID check:" && test -n "$S3_ACCESS_KEY_ID" && echo "✅ S3_ACCESS_KEY_ID EXISTS" || echo "❌ S3_ACCESS_KEY_ID MISSING"
        - echo "GOOGLE_CLIENT_ID check:" && test -n "$GOOGLE_CLIENT_ID" && echo "✅ GOOGLE_CLIENT_ID EXISTS" || echo "❌ GOOGLE_CLIENT_ID MISSING"
        - echo "========================================="
        - echo "📦 Installing dependencies..."
        - npm ci
        - echo "🔧 Generating Prisma client..."
        - npx prisma generate
        - echo "✅ PREBUILD COMPLETED SUCCESSFULLY"
    build:
      commands:
        - echo "🏗️ STARTING BUILD PHASE..."
        - npm run build
        - echo "✅ BUILD COMPLETED SUCCESSFULLY"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*

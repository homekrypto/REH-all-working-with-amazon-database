version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 OPTIMIZED AMPLIFY BUILD STARTED"
        - echo "Node.js version: $(node --version)"
        - echo "NPM version: $(npm --version)"
        - echo "🔍 Critical env vars:"
        - 'test -n "$DATABASE_URL" && echo "✅ DATABASE_URL" || echo "❌ DATABASE_URL"'
        - 'test -n "$NEXTAUTH_SECRET" && echo "✅ NEXTAUTH_SECRET" || echo "❌ NEXTAUTH_SECRET"'
        - 'test -n "$S3_ACCESS_KEY_ID" && echo "✅ S3_ACCESS_KEY_ID" || echo "❌ S3_ACCESS_KEY_ID"'
        # Use npm ci for faster, reliable installs
        - echo "📦 Installing dependencies with npm ci..."
        - npm ci --production=false --prefer-offline --no-audit --no-fund
        # Generate Prisma client early
        - echo "🔧 Generating Prisma client..."
        - npx prisma generate
        - echo "✅ Dependencies installed"
    build:
      commands:
        - echo "🏗️ Building Next.js application..."
        # Set Node.js memory limit for large builds
        - export NODE_OPTIONS="--max-old-space-size=4096"
        # Verify critical env vars one more time
        - 'test -n "$DATABASE_URL" || (echo "❌ DATABASE_URL missing" && exit 1)'
        - 'test -n "$NEXTAUTH_SECRET" || (echo "❌ NEXTAUTH_SECRET missing" && exit 1)'
        # Build with optimizations
        - npm run build
        - echo "✅ Build completed successfully"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
      - ~/.npm/**/*
